export type DashboardType = {
    totalOrders: number,
    totalReviews: number,
    totalCourses: number,
    totalStudents: number
}

export type StatisticProductType = {
    stt?: number
    course: string,
    quantity: 1,
    price: number
}